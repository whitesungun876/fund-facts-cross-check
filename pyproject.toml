[project]
name = "fund-facts-cross-check"
version = "0.1.0"
description = "Two-model fact comparison with bounded, source-grounded verification"
requires-python = ">=3.12,<3.13"
dependencies = ["pydantic>=2.10,<3", "httpx>=0.28,<0.29"]

[tool.uv]
package = false
